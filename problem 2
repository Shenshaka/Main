#include <iostream>
#include <math.h>
using namespace std;

int main()
{    
   long n,i=0;
   cin >> n;
   if (n < 0){
       n = -n;
   }
   do{
       n /= 10;
       i++;
   } while (n != 0);
   cout << i << endl;
   return 0;
}
